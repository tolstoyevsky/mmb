##
# You should look at the following URL's in order to grasp a solid understanding
# of Nginx configuration files in order to fully unleash the power of Nginx.
# http://wiki.nginx.org/Pitfalls
# http://wiki.nginx.org/QuickStart
# http://wiki.nginx.org/Configuration
#
# Generally, you will want to move this file somewhere, and start with a clean
# file but keep this around for reference. Or just disable in sites-enabled.
#
# Please see /usr/share/doc/nginx-doc/examples/ for more detailed examples.
##

# Default server configuration
#
server {
	listen PORT default_server;
	listen [::]:PORT default_server;

	# SSL configuration
	#
	# listen 443 ssl default_server;
	# listen [::]:443 ssl default_server;
	#
	# Self signed certs generated by the ssl-cert package
	# Don't use them in a production server!
	#
	# include snippets/snakeoil.conf;

	root /var/www;

	# Add index.php to the list if you are using PHP
	index index.html index.htm index.php;

	server_name _;

	# Should be equal or close to upload_max_filesize in .user.ini.
	client_max_body_size 1536M;
	# Should be equal to upload_tmp_dir in php.ini.
	client_body_temp_path /var/www/nc/data;

	location /oc {
		rewrite ^/\.well-known/host-meta /public.php?service=host-meta break;
		rewrite ^/\.well-known/host-meta\.json /public.php?service=host-meta-json break;
		rewrite ^/\.well-known/carddav /remote.php/carddav/ redirect;
		rewrite ^/\.well-known/caldav /remote.php/caldav/ redirect;
	}

	location /oc/apps {
		rewrite ^/apps/calendar/caldav\.php /remote.php/caldav/ break;
		rewrite ^/apps/contacts/carddav\.php /remote.php/carddav/ break;
	}

	location /oc/data {
		deny all;
	}

	location /oc/remote {
		rewrite ^/remote/(.*) /remote.php break;
	}

	location ~ \.php(?:$|/) {
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
		include fastcgi_params;
		fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
		fastcgi_param PATH_INFO $fastcgi_path_info;
		# Should be equal to max_execution_time in php.ini.
		fastcgi_read_timeout 1800s;
		fastcgi_pass unix:/var/run/php5-fpm.sock;

		include snippets/fastcgi-php.conf;
	}

	# deny access to .htaccess files, if Apache's document root
	# concurs with nginx's one
	#
	#location ~ /\.ht {
	#	deny all;
	#}
}


# Virtual Host configuration for example.com
#
# You can move that to a different file under sites-available/ and symlink that
# to sites-enabled/ to enable it.
#
#server {
#	listen 80;
#	listen [::]:80;
#
#	server_name example.com;
#
#	root /var/www/example.com;
#	index index.html;
#
#	location / {
#		try_files $uri $uri/ =404;
#	}
#}
